export default defineAppConfig({
    ui: {
        // A chave 'notifications' foi removida pois causa conflito de tipos na v3.
        // As notificações padrão já funcionam automaticamente via <UApp>.
    }
})